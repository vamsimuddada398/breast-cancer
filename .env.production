# Breast Cancer Detection System - Backend Production
# Environment configuration for production deployment

# Server Configuration
HOST=0.0.0.0
PORT=8000
ENVIRONMENT=production

# Model Paths (adjust based on deployment structure)
MODEL_PATH=/app/models/efficientnet_final.h5
UNET_MODEL_PATH=/app/models/unet_segmentation.h5

# CORS Settings (restrict to frontend URL in production)
CORS_ORIGINS=["https://your-frontend-url.vercel.app"]

# Logging
LOG_LEVEL=WARNING

# AWS S3 Configuration (if using for model storage)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_REGION=us-east-1
# MODEL_S3_BUCKET=your-bucket-name
